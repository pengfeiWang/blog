#冒泡排序

```
var arr = [10,20,33,8,5,22,19,90,2];
var len = arr.length;
for( var i = 0; i < len; i++ ) {
  for( var j = i+1; j < len; j++ ) {
    if( arr[i] > arr[j] ) {
      var temp = arr[i];
      arr[i] = arr[j];
      arr[j] = temp
    }
  }
}
```